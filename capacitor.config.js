/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: 'com.xenvia.app',
  appName: 'Xenvia',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
